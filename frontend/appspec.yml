version: 0.0
os: linux
files:
  - source: /
    destination: /usr/share/nginx/html  # Path where Nginx serves the files
    overwrite: yes
hooks:
  BeforeInstall:
    - location: scripts/cleanup.sh  # Script to remove old files
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/restart_nginx.sh  # Script to restart Nginx
      timeout: 300
      runas: root

# # Create a new appspec.yml with inline scripts
# version: 0.0
# os: linux
# files:
#   - source: /frontend
#     destination: /usr/share/nginx/html
#     overwrite: yes
# hooks:
#   BeforeInstall:
#     - location: scripts/cleanup.sh
#       timeout: 300
#       runas: root
#       content: |
#         #!/bin/bash
#         echo "Cleaning up old files..."
#         rm -rf /usr/share/nginx/html/*
#   AfterInstall:
#     - location: scripts/restart_nginx.sh
#       timeout: 300
#       runas: root
#       content: |
#         #!/bin/bash
#         echo "Restarting Nginx..."
#         systemctl restart nginx || service nginx restart
